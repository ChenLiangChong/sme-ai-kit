# v2.4 確認審查報告

## 總結
- 🔴 必修：4
- 🟡 可修：5
- ✅ 完全通過：16 檔

## 問題（只列有問題的）

### 🔴 必修

1. **檔名**：`competitive-content.md`
   **行號**：L262-L270
   **問題描述**：`Output Artifacts（產出物）` 表格仍為 3 欄（`產出 | 格式 | 說明`），不符合 `style-guide.md` 規定的固定兩欄格式：`使用者要什麼 | Claude 給什麼`。
   **建議修法**：改成兩欄表，將「格式／說明」合併進 `Claude 給什麼` 欄位描述。

2. **檔名**：`paid-acquisition.md`
   **行號**：L605-L613
   **問題描述**：`Output Artifacts（產出物）` 表格表頭使用 `產出物 | 說明`，未依 `style-guide.md` 統一為 `使用者要什麼 | Claude 給什麼`。
   **建議修法**：改為固定兩欄表頭，並把目前右欄內容改寫成「Claude 給什麼」的交付描述。

3. **檔名**：`pmm-launch.md`
   **行號**：L217-L225
   **問題描述**：`Output Artifacts（產出物）` 以 `###` 出現，且表格仍為 3 欄（`產出物 | 格式 | 說明`）；不符合標準結構與固定兩欄規則。
   **建議修法**：提升為 `## Output Artifacts（產出物）`，並改成 `使用者要什麼 | Claude 給什麼` 兩欄表格。

4. **檔名**：`pmm-launch.md`
   **行號**：L153-L155
   **問題描述**：`Go/No-Go 決策框架` 仍是英文在前，違反 v2.2 雙語規則「中文在前、英文括號補充」。
   **建議修法**：改為 `可上線／不可上線（Go/No-Go）決策框架`；表格欄名已正確，可沿用。

### 🟡 可修

5. **檔名**：`social-content.md`
   **行號**：L389、L405、L519
   **問題描述**：`Social Proof` 對應詞寫成「社會證明」，與 `terminology.md` / `term-gloss.md` 規定的「社群背書」不一致。
   **建議修法**：統一改為「社群背書」。

6. **檔名**：`copy-editing.md`
   **行號**：L497
   **問題描述**：`Social Proof` 概念寫成「社會證明」，與全域術語「社群背書」不一致。
   **建議修法**：改為「社群背書」。

7. **檔名**：`competitive-content.md`
   **行號**：L416
   **問題描述**：寫成「缺乏社會證明」，與同檔前文 L328 的 `Social Proof（社群背書）` 及術語表不一致。
   **建議修法**：改為「缺乏社群背書」。

8. **檔名**：`paid-acquisition.md`
   **行號**：L622
   **問題描述**：寫成「暖受眾用社會證明」，與本專案統一術語「社群背書」不一致。
   **建議修法**：改為「暖受眾用社群背書」。

9. **檔名**：`pmm-market.md`
   **行號**：L155、L181
   **問題描述**：同一概念在 PMM 檔群中已有 `Persona（人物誌）` / `買家人物誌` 定義，但此檔仍使用 `Buyer Personas`、`Buyer Persona`，造成跨檔術語漂移。
   **建議修法**：統一改為 `買家人物誌（Buyer Persona）` 或純中文 `買家人物誌`，並與 `pmm-messaging.md` 保持一致。

## 整體觀察

- 使用者列出的 v2.3 已修項目，本輪未發現回歸。
- 大多數檔案已穩定；問題主要集中在 2 類：`Output Artifacts` 結構未完全收斂，以及 `Social Proof` / `Persona` 類術語的跨檔漂移。
- `pmm-*` 系列仍有少量雙語順序與章節結構未完全對齊 `style-guide.md`，建議優先把固定模板區塊收斂，再做零星術語清理。

## 最終評級
**可以上線：否**

